# Ponasa Landing Page

Quick static redesign for Ponasa Real Estate Investments & Management.

## Local Preview

Run `run-local.bat` and open `http://localhost:8000`.

The site is static HTML/CSS/JS, so it can be hosted by any static web host.
